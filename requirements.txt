# Optional: Only needed for USB microscope support
# WiFi mode works without any additional packages

# For USB camera support
opencv-python>=4.5.0

# For better JPEG encoding (reduces corruption warnings)
Pillow>=8.0.0
